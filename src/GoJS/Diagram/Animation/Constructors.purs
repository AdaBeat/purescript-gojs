module GoJS.Diagram.Animation.Constructors where

import Effect (Effect)
import GoJS.Diagram (Animation_)
import GoJS.Unsafe (constructor0)

newAnimation :: Effect Animation_
newAnimation = constructor0 "Animation"
